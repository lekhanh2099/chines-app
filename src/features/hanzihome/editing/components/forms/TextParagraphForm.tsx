import { textParagraphEditAdapter } from "../../adapters/textParagraphEditAdapter";
import { createNodeForm } from "./createNodeForm";

export const TextParagraphForm = createNodeForm(textParagraphEditAdapter);
